- In optim_test, third test param settings contain param2
- if libraries cannot be read, advise build NOMAD and relaunch Julia (in warning)
- right granularity check : gr[i]==0 || (x[i]<1e-14) || Int(round(x[i]/gr[i],digits=13))
